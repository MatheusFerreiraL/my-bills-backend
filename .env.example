NODE_ENV=development
PORT=3000

# Postgres connection string. Row-level security (AD-3) is enforced by policies keyed off
# app.current_user_id, set per-request via src/infra/database/tenant-context.ts.
DATABASE_URL=postgres://mybills:mybills@localhost:5432/mybills
